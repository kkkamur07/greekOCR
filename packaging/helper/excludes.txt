# PyInstaller excludes for the Inference Helper bundle.
# One import name per line; lines starting with # are ignored.
# Goal: ship only Calamari and BLLA ONNX inference on CPU.

torch
torchgen
torchvision
safetensors
kraken
matplotlib
notebook
jupyter
IPython
tkinter
PyQt5
PyQt6
PySide2
PySide6
tensorflow
tensorboard
pandas
nomicous
alembic
asyncpg
pytest
transformers
accelerate
datasets
gradio
# Postgres drivers and ORM (the helper is sync /run only, and since ADR 0003 no
# part of `inference` talks to a database at all)
psycopg2
sqlalchemy
inference.api.dependencies
inference.api.main
src.hf.publish
# Native model implementations are retained for training/export/parity, but
# must never enter the frozen helper's import graph.
src.model.inference_export
inference.architectures.blla.blla
inference.architectures.blla.blla_model
inference.architectures.calamari.config
inference.architectures.calamari.layers
inference.architectures.calamari.model
