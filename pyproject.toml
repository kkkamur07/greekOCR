[project]
name = "greek-ocr"
version = "0.1.0"
description = "We are going to digitize the whole Greek Legal manuscripts"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.51.0",
    "transformers>=4.57.1",
]
