models:
  syriac-calamari-v1:
    task: transcribe
    architecture: calamari
    device: cpu
    host_eligibility: local
    versions:
      stable:
        weights_source: hf://kkkamur07/syriac-htr-calamari@stable
        hub_revision: 5ff715e873f1ae3f325ebea4d2c4a95eb5094601
        # best.pt at that revision. The digest moved from best.onnx to the
        # native checkpoint when ADR 0004 made PyTorch the runtime; the
        # revision did not, because both artifacts were published together.
        artifact_sha256: ea711b918010aa31bd4a8a5de99c7953207421a7c7d4a39163166db380013053

  # greek-calamari-v1 is NOT shippable yet: Hub repo kkkamur07/greek-htr-calamari
  # does not exist (404). Uncomment only after publishing weights + pinning
  # hub_revision + artifact_sha256 (same pattern as syriac-calamari-v1).
  # greek-calamari-v1:
  #   task: transcribe
  #   architecture: calamari
  #   device: cpu
  #   host_eligibility: local
  #   versions:
  #     stable:
  #       weights_source: hf://kkkamur07/greek-htr-calamari@stable
  #       hub_revision: <40-char-commit>
  #       artifact_sha256: <sha256-of-best.pt>

  blla-segment:
    task: segment
    architecture: blla
    device: cpu
    host_eligibility: local
    versions:
      stable:
        weights_source: hf://kkkamur07/segmentation-blla@stable
        hub_revision: 444d51dd7b34cd2012b1ffe1c9c9442c875d8230
        # blla.safetensors at that revision (see the Calamari note above).
        artifact_sha256: 8b5b6ec21947d3c5a7c117c7b873f3a923aab48950ba05707805899877c397ce
